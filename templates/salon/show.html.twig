{% extends 'base.html.twig' %}

{% block title %}Salon{% endblock %}

{% block body %}
    <h1>Salon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salon.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ salon.Description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ salon.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salon_index') }}">back to list</a>

    <a href="{{ path('app_salon_edit', {'id': salon.id}) }}">edit</a>

    {{ include('salon/_delete_form.html.twig') }}
{% endblock %}
